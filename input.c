#include<stdio.h>
int main(void)
{
  float length, breadth, area;
  length = 10;
  breadth = 20;
  area= length * breadth;
  printf("the area of the rectangle with %f as breadth and %f as length %f\n",length,breadth,area);
  return 0;
}